# 合并方案 A：统一入口 · 双模块 · 各自云存储

> 决策日期：2026-09-01，用户确认采用方案 A。
> 决策更新：2026-09-01 17:04，用户拍板「词根暂不升 Pro、词根完整版暂不上线」→ 合并工作暂不启动，当前仅推进长难句孩子数据迁移。

## 方案 A 定义

- **一个 PWA 站点**，主域名 `englearning.cc` 下，顶部 Tab 切换「词根 / 长难句」两个模块
- **全部部署到 Cloudflare Pages**（免费、无冷启动、国内已实测可用）
- **存储各自保留不动**：
  - 词根模块 → Supabase（Postgres + Auth，项目 `khthjbutmzopfesvoqjk.supabase.co`）
  - 长难句模块 → Cloudflare Worker + R2（`lcs-families` 桶）
- **零数据迁移、最低风险**——不重写任何一方的数据/账户层

## 现状盘点（决策时）

| 项目 | 线上状态 | 存储 | 备注 |
|---|---|---|---|
| 长难句打卡 | ✅ `app.englearning.cc` 已上线（Worker+R2+Pages），运行正常 | R2 `lcs-families` | 孩子数据迁移待用户手动完成 |
| 词根 | ⚠️ Render 跑的是旧版 `pwa/`（45KB，无 Supabase） | 旧版无云存储 | 完整版 `app/index.html`（528KB）含 Supabase 配置但**未部署** |

### 词根的两个关键事实
1. 完整版 `app/index.html` 已配好 Supabase（`auth.signUp/signIn`、`user_data` 表 upsert），只是没上线 → **词根"上云"本身只需部署，不用再开发**。
2. Supabase **免费层一周无活动自动暂停** → 家庭偶尔用的词根应用可能被休眠。决策：**暂不升 Pro**（接受暂停风险，因完整版暂不上线，暂无线上用户受影响）。若将来上线，再评估是否升 Pro。

## 落地步骤（建议顺序）

> ⏸ **当前状态（2026-09-01 17:04）**：步骤 1（词根完整版上线）**暂缓**，步骤 3–5（合并外壳）**暂不启动**。
> 原因：用户确认词根暂不升 Pro、完整版暂不上线。当前唯一推进项是步骤 2（孩子长难句数据迁移，用户手动）。

1. ~~**词根 Supabase 完整版先上线**~~ → **暂缓，未定档期**
   - 部署 `词根/app/` 到新 Pages 项目（或暂挂 `roots.englearning.cc`）
   - 验证 Supabase 注册/登录/读写
   - 词根升 Pro：已决定暂不升（待上线时再议）
2. **【进行中·用户手动】孩子长难句数据迁移**：旧版导出 → `app.englearning.cc` 导入恢复
3. **搭合并外壳站点**（新 Pages 项目，不立即切换生产流量）→ **暂缓**
   - 单页含顶部 Tab：词根 / 长难句
   - 长难句模块：复用现有 `app.englearning.cc` 逻辑（可 iframe 或抽组件）
   - 词根模块：加载上线后的词根（步骤 1 完成后）
   - 同源下 localStorage key 不冲突即可
4. **切换域名**：主域 `englearning.cc` 指向合并站；`app.englearning.cc` 可保留给长难句独立访问或重定向 → **暂缓**
5. **验证 + 旧版退役**（Render 旧服务停用）→ **暂缓**

## 前置依赖 / 待决项

- [ ] 孩子长难句数据迁移完成（用户手动，避免合并切换干扰）— **当前唯一进行项**
- [x] 词根 Supabase 完整版上线并验证 — **决定：暂缓，暂不上线**
- [x] 词根是否升 Pro — **决定：暂不升（接受免费层暂停风险）**
- [ ] 合并站 URL 结构（主域 vs app 子域分工）— 待词根上线后再议
- [ ] 合并站 Tab 切换是用 iframe 嵌入还是代码级组件复用（影响改造成本）— 待词根上线后再议

## 为什么不强行统一存储（方案 B）

两应用范式不同：词根=个人账户+结构化 SQL；长难句=家庭共享+JSON 对象。统一到一套存储需重写其中一个的数据/账户层（中等工作量），对家庭小工具收益低。方案 A 用"保留存储、只合入口"规避了这笔成本。

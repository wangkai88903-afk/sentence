# 方案 B 上线前置清单：你还需提供什么

> 起点：仅注册了 Cloudflare 账号，其他什么都没做。
> 日期：2026-09-01

---

## 0. 一句话结论

除了账号，还差三样需要**你本人**参与的准备：① 在本机完成 Cloudflare 登录授权；② 确认账号已绑卡/支付方式验证（R2 的隐形门槛）；③ 一个**自有域名**并接入 Cloudflare（国内可达性关键）。其余代码、配置、命令全部由我产出。

---

## 1. 已具备

- ✅ Cloudflare 账号（已注册）

---

## 2. 你必须本人提供 / 操作（涉及账号安全，我不能代劳）

| # | 项目 | 为什么必须你做 | 怎么做 |
|---|---|---|---|
| ① | **wrangler 登录授权** | 需要你的账号密码 + 2FA，会打开浏览器 OAuth | 本机装好 Node 后跑 `npx wrangler login`，浏览器里授权 |
| ② | **支付方式 / 绑卡验证** | Cloudflare 现在创建 R2 桶通常要求验证支付方式（免费额度内不扣费） | 在 Billing 页面绑定信用卡 / 支付宝等；验证后不消费就不扣钱 |
| ③ | **自有域名 + 接入 Cloudflare** | 域名要你购买，DNS 要改 NS 指向 Cloudflare | 见第 4 节（★ 国内必需） |

> 第 ③ 项若用 Cloudflare 自带的 `*.workers.dev` 子域，**国内访问极不稳定**（被 GFW 干扰），客户端同步会经常失败。所以强烈建议用自有域名。

---

## 3. 可让我代定 / 你只需确认

- **桶名**：建议 `lcs-families`
- **Worker 名称 / 子域前缀**：建议 `lcs-checkin-sync`
- **并发策略**：已定 **ETag 条件写 + 重试**（乐观锁）
- **静态托管**：Pages（推荐）；若嫌麻烦也可让 Worker 顺带托管静态

---

## 4. ★ 自有域名怎么弄（国内必需）

1. 在任意注册商买一个域名（阿里云、腾讯云、NameSilo、Cloudflare Registrar 等，一年几十元起）。
2. 在 Cloudflare 控制台 **Add a Site**，填入你的域名 → Cloudflare 给你两个 NS 地址。
3. 回原注册商把域名的 Nameserver 改成 Cloudflare 给的那两个（生效通常几分钟~几小时）。
4. 之后该域名的解析全在 Cloudflare 管，免费。
5. 给 Worker 配自定义路由，例如 `api.yourdomain.com`；给 Pages 配 `app.yourdomain.com`。
6. 这样前端与 API 同属你的域名，**免 CORS、国内可达**。

> 不买域名的替代方案：用 `*.workers.dev`，但国内基本不可用，仅适合纯海外/测试。

---

## 5. 我负责产出（你不用管）

- `worker.js`：移植 `server.js` 的 API 逻辑 + R2 绑定 + 乐观锁
- `wrangler.toml`：绑定 R2 桶
- 前端 `index.html`：`cloudFetch` 的 base URL 改造（指向你的域名）
- 部署脚本 + 完整命令清单（`wrangler dev` / `wrangler deploy` / Pages 部署）
- 多端并发联调验证方案

---

## 6. 国内网络特别提醒

- **客户端运行时**：Worker/Pages 必须挂在你自己的域名下，否则 `*.workers.dev` 不可达。
- **部署时本机**：`wrangler` 部署、首次 `login` 都需要本机能稳定访问 Cloudflare（必要时用代理）。
- R2 桶本身无地域限制，数据读写经 Cloudflare 边缘，国内访问正常。

---

## 7. 最小启动清单（你照着做）

- [ ] 确认 Cloudflare 账号已**绑卡/支付验证**（否则建不了 R2 桶）
- [ ] 买一个自有域名（若没有）
- [ ] 域名接入 Cloudflare（Add Site + 改 NS）
- [ ] 本机：`node -v` 确认有 Node → `npx wrangler login` 完成授权
- [ ] 告诉我：桶名/子域前缀是否沿用默认、域名是什么
- [ ] 我产出全部代码与命令 → 你执行 `wrangler deploy`

---

## 8. 推荐 vs 最小路径

- **推荐路径**：账号绑卡 → 自有域名接入 CF → Worker+Pages 都挂你的域名 → 同源免 CORS、国内稳。
- **最小路径**（仅验证可行性）：账号绑卡 → 不买域名 → 用 `*.workers.dev` 先跑通逻辑（国内客户端不可用，仅作技术验证）。

---

## 9. 合规与国内可达性确认（购买前核验，2026-09-01）

用户购前咨询，经检索多方来源确认：

- **备案**：CF Pages/Workers/R2 源站均在境外（新加坡/日本/香港等），**不触发 ICP 备案**。ICP 备案的强制条件是「服务器物理位置在中国大陆境内」。家庭自用、非经营性学习工具，合规风险极低。
- **国内访问（主要风险点）**：Cloudflare 在大陆无节点，走周边节点，延迟约 50–150ms（华东/华南），部分运营商/时段偶发不稳。关键避坑：**必须绑定自有域名，绝不用 `*.workers.dev` / `*.pages.dev`**（自 2022 起常被 DNS 污染/阻断）。
- **`.cc` 后缀**：科科斯群岛 ccTLD（商业化运营），国内认知度较高、解析稳定、被标记概率低，家庭工具无影响。
- **结论**：可放心购买 `englearning.cc`。若日后移动网络偶发不稳，可加国内 CDN 前置（腾讯云 EdgeOne / DNSPod 智能解析）作优化，非必需。
